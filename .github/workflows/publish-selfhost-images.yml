name: Publish self-host Docker images

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Docker image tag, for example selfhost-20260622-1"
        required: true
        type: string
  push:
    tags:
      - "selfhost-*"

permissions:
  contents: read
  packages: write

concurrency:
  group: publish-selfhost-images
  cancel-in-progress: false

env:
  IMAGE_REGISTRY: ghcr.io/xinyifu
  PLATFORM: linux/amd64
  UPSTREAM_IMAGE_TAG: latest

jobs:
  resolve:
    name: Resolve release metadata
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - name: Resolve image tag
        id: meta
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${GITHUB_REF:-}" == refs/tags/* ]]; then
            image_tag="${GITHUB_REF_NAME}"
          else
            image_tag="${{ inputs.image_tag }}"
          fi

          if [[ ! "${image_tag}" =~ ^[A-Za-z0-9_][A-Za-z0-9_.-]{0,127}$ ]]; then
            echo "Invalid Docker tag: ${image_tag}" >&2
            exit 1
          fi

          echo "image_tag=${image_tag}" >> "${GITHUB_OUTPUT}"

  build_fork_image:
    name: Build fork image (${{ matrix.image }})
    needs: resolve
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: backend-server
            image: backend-server
          - target: room-server
            image: room-server
          - target: web-server
            image: web-server
          - target: init-db
            image: init-db
          - target: init-selfhost-overrides
            image: init-selfhost-overrides
          - target: openresty
            image: openresty
    steps:
      - name: Fetch sources
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.image }}
        env:
          IMAGE_TAG: ${{ needs.resolve.outputs.image_tag }}
          SEMVER_FULL: ${{ needs.resolve.outputs.image_tag }}
          CACHE_REGISTRY: ghcr.io/xinyifu/apitable/build-cache
          BAKE_TARGET: ${{ matrix.target }}
          IMAGE_NAME: ${{ matrix.image }}
        shell: bash
        run: |
          set -euo pipefail

          docker buildx bake --push \
            --set "${BAKE_TARGET}.platform=${PLATFORM}" \
            --set "${BAKE_TARGET}.cache-from=type=registry,ref=${CACHE_REGISTRY}:${BAKE_TARGET}" \
            --set "${BAKE_TARGET}.cache-to=type=registry,ref=${CACHE_REGISTRY}:${BAKE_TARGET},mode=max" \
            --set "${BAKE_TARGET}.tags=${IMAGE_REGISTRY}/apitable/${IMAGE_NAME}:${IMAGE_TAG}" \
            "${BAKE_TARGET}"

  mirror_upstream_image:
    name: Mirror upstream image (${{ matrix.image }})
    needs: resolve
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        image:
          - databus-server
          - imageproxy-server
          - init-appdata
    steps:
      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull, retag, and push ${{ matrix.image }}
        env:
          IMAGE_TAG: ${{ needs.resolve.outputs.image_tag }}
          IMAGE_NAME: ${{ matrix.image }}
        shell: bash
        run: |
          set -euo pipefail

          source="docker.io/apitable/${IMAGE_NAME}:${UPSTREAM_IMAGE_TAG}"
          target="${IMAGE_REGISTRY}/apitable/${IMAGE_NAME}:${IMAGE_TAG}"

          docker pull --platform "${PLATFORM}" "${source}"
          docker tag "${source}" "${target}"
          docker push "${target}"

  publish_summary:
    name: Publish summary
    needs:
      - resolve
      - build_fork_image
      - mirror_upstream_image
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - name: Write self-host release summary
        env:
          IMAGE_TAG: ${{ needs.resolve.outputs.image_tag }}
          BUILD_RESULT: ${{ needs.build_fork_image.result }}
          MIRROR_RESULT: ${{ needs.mirror_upstream_image.result }}
        shell: bash
        run: |
          cat <<EOF >> "${GITHUB_STEP_SUMMARY}"
          ## Self-host release images

          | Item | Value |
          | --- | --- |
          | Image tag | \`${IMAGE_TAG}\` |
          | Registry | \`ghcr.io/xinyifu\` |
          | Platform | \`${PLATFORM}\` |
          | Fork image build | \`${BUILD_RESULT}\` |
          | Upstream image mirror | \`${MIRROR_RESULT}\` |

          ## Published image groups

          | Image | Source |
          | --- | --- |
          | \`backend-server\` | fork build |
          | \`room-server\` | fork build |
          | \`web-server\` | fork build |
          | \`init-db\` | fork build |
          | \`init-selfhost-overrides\` | fork build |
          | \`openresty\` | fork build |
          | \`databus-server\` | upstream mirror |
          | \`imageproxy-server\` | upstream mirror |
          | \`init-appdata\` | upstream mirror |

          ## Deployment note

          Use the matching self-host config for this tag. This release requires \`init-selfhost-overrides\` to run after \`init-appdata\`.

          ## Upgrade command reference

          \`\`\`powershell
          docker compose --env-file .env -f docker-compose.yaml pull
          docker compose --env-file .env -f docker-compose.yaml down
          docker compose --env-file .env -f docker-compose.yaml up -d
          docker compose --env-file .env -f docker-compose.yaml ps
          \`\`\`

          EOF

      - name: Fail summary when publish failed
        if: ${{ needs.build_fork_image.result != 'success' || needs.mirror_upstream_image.result != 'success' }}
        run: exit 1
