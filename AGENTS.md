# AGENTS.md

- Prefer minimal changes.
- Preserve existing code style and project structure.
- Avoid unnecessary refactoring, abstraction, or new dependencies.
- Consult documentation before using APIs or frameworks.
- Do not assume undocumented behavior.
- Produce directly executable output.

## Repository Notes

- This fork is for self-hosted deployments only.
- Push changes to `origin` (`xinyifu/apitable`) by default.
- Treat `upstream` (`apitable/apitable`) as read-only for fetch/pull sync; do not push to upstream.
